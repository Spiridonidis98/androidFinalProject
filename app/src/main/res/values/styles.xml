<resources>
    <!-- Other styles -->
    <style name="CustomDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowEnterAnimation">@anim/slide_in_up</item>
        <item name="android:windowExitAnimation">@anim/slide_out_down</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <style name="MyDatePickerDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <!-- Customize your dialog here -->
        <item name="colorAccent">@color/primarycolor</item> <!-- Accent color for the dialog -->
        <item name="android:textColorPrimary">@color/black</item> <!-- Text color for primary text -->
        <item name="android:textColorSecondary">@color/white</item> <!-- Text color for secondary text -->
    </style>

    <style name="BottomNavigationViewStyle" parent="Widget.Design.BottomNavigationView">
        <item name="itemIconTint">@color/white</item>
        <item name="itemTextColor">@color/primarycolor</item>
        <item name="android:background">?android:attr/windowBackground</item>
    </style>
</resources>