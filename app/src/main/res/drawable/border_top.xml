<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Transparent background -->
    <corners android:topLeftRadius="16dp" android:topRightRadius="16dp" /> <!-- Adjust the radius as needed -->
    <stroke android:width="0dp" android:color="@android:color/darker_gray" /> <!-- Border color and width -->
</shape>
